dependencies:
  pre:
    - bundle install
    - bower install
  bundler:
    without:
      - development

deployment:
  production:
    branch: master
    commands:
      # Middleman build
      - bundle exec middleman deploy --verbose
